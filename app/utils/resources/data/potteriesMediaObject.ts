import type { StaticResourceConfig } from '~~/types/api'

export default {
  appPath: '/',
  labels: ['Medium (Pottery)', 'Media (Pottery)'],
  defaultHeaders: [],
} as Readonly<StaticResourceConfig>
